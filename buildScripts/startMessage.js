// var chalk = require('chalk'); // a library that allows to specify the color of the output
import chalk from 'chalk'; // a standart that was implemented in ES6


console.log(chalk.green('Starting app in dev mode...'));